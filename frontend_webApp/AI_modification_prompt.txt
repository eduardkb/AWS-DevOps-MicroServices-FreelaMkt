Modify Python Flask Web application code attached as app.zip.
use as few AI resources as possible.
don't write text explaining the code. while it is being written.
priority is to generate the code requested code below.
Always zip only the files that needed to be modified to the results.zip file and let me download it.

Modifications:
    - Currently the container is freezing and not loading because the API call times out.
    - make sure that timeout happens only after 60 seconds try.
    - show the page no matter what before the api call and make the site show to the user that somehting is being done (placeholder blinking, progress bar or spinning wheel)
    - if api fails, display message why it failed
