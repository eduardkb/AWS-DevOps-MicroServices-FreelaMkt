Modify Python Flask Web application code attached as app.zip.
use as few AI resources as possible.
don't write text explaining the code. while it is being written.
priority is to generate the code requested code below.
Always zip only the files that needed to be modified to the results.zip file and let me download it.

Modifications:
    - Problem: This code is deployed in a AWS ECS inside a private subnet. 
    - So, parts of the authentication workflow does not work because ECS needs Internet access which cannot be granted.
    - modify this applciation's logic so that the whole authentication workflow is done on the frontend (user's browser)
    - following best practices, securely store the tokens locally
    - Exchange the authorization code for tokens directly from the browser.
    - Handle token expiration and refresh using the refresh token.
    - Implement logout by redirecting the browser to the Cognito logout endpoint and clearing local sessions.